[Unit]
Description=Arm-only gamepad test (base commands disabled)
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash -lc 'source /opt/ros/jazzy/setup.bash && ros2 run joy joy_node & exec python3 %h/gamepad_teleop.py --ros-args -r /teleop/cmd_vel:=/arm_test/disabled_cmd_vel'
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
